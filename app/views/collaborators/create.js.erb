<% if @wiki.collaborator.valid? %>
  $('.js-collaborators').prepend("<%= escape_javascript(render(@wiki.collaborator)) %>");
  $('#user-' +<%= @user.id %>).hide();
<% end %>

<% if @wiki.collaborators.include? user %>
  $('.non-collaborator').addClass("hidden");
<% end %>